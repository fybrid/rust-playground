# Todo CLI

Manage todo items from the command line and persist them as JSON.

You will practice structs, serialization, file persistence, and small command dispatch. Extend this project by adding due dates, priorities, or filtering.

