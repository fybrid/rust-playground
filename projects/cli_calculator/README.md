# CLI Calculator

Build a command-line calculator that parses simple expressions like `2 + 3`.

You will practice input parsing, enums, `Result`, and user-facing error messages. Extend this project by supporting more operators, better whitespace handling, and decimal values.

