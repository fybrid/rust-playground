# JSON Config Loader

Load application configuration from JSON and validate the required fields.

You will practice `serde`, JSON parsing, configuration structs, and error boundaries. Extend this project by supporting environment overrides or multiple config files.

