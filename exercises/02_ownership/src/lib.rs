pub fn normalize_title(title: String) -> String {
    todo!("trim the title and convert it to lowercase")
}

pub fn total_bytes(lines: Vec<String>) -> usize {
    todo!("consume the vector and return the total byte length")
}

pub fn append_suffix(words: Vec<String>, suffix: &str) -> Vec<String> {
    todo!("return a new vector where every word has the suffix appended")
}

pub fn keep_original_and_uppercase(name: String) -> (String, String) {
    todo!("return the original name and an uppercase copy")
}

